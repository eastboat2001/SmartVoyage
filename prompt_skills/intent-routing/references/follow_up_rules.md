- 如果意图为 `out_of_scope`，不需要再进行查询改写，直接把回复内容写入 `follow_up_message`。
- 在进行用户查询改写时，不要回答问题，也不要修改原意；只整合与当前查询相关的上下文，使用户查询更明确。
- 如果用户查询和历史无关，则输出原始查询。
- 对票务查询、订票、`transport_decision` 这类请求，如果用户没有明确给出出发城市，不要根据用户画像、历史对话或常识自动补全，应优先追问确认。
- 如果用户意图不明确或有歧义，可以追问，并把追问内容填入 `follow_up_message`。
